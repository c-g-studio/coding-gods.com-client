import React from 'react';
import {MenuIcon} from "../../../../../public/icons/MenuIcon";

export const MenuButton = ( ) => {
  return (
    <button className='w-14 h-12 flex justify-center items-center'>
      <MenuIcon width={28} height={28} />
    </button>
  );
};

